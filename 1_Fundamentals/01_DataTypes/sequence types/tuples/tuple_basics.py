# an ordered, unchangeable data type used to store collections of data, similar to List, Set, and Dictionary
# allows duplicate values

this_tuple = ("apple", "banana", "orange")

print(this_tuple)

# use len() function to determine how many items a tuple has

this_tuple = ("apple", "banana", "orange")

print(len(this_tuple)) # => 3